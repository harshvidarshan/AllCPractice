#include<stdio.h>
int main()
{
	int a,b;
	printf("Enter the value a: ");
	scanf("%d",&a);
    printf("\n Enter the value b: ");
	scanf("%d",&b);
	
if(a>b)
{

printf("the first number is larger than second number");
}
else
{

printf("the second number is larger than first number");
}

return 0;
	
}
