#include <stdio.h>

int main(void) {
	// your code goes here
    int t,a,rev,m;
    scanf("%d",&t);
    while(t--)
    {
        scanf("%d",&a);
        
    }
    for(i=a;i<=)
    while(a!=0)
        {
            m=a%10;
            rev=(rev*10)+m;
            a=a/10;
        }
        printf("\n%d ",rev);
	return 0;
}
