#include<stdio.h>
#include<ctype.h>
int main()
{	
	char ch;
	char alpha;
	int number;
	printf("Enter alpha: ");
	scanf("%c",&alpha);
	
	isalpha(ch)? (("printf alpha is an alphabet character")):(("printf number is not alphabet character"));
	printf("Enter number: ");
	scanf("%d",&number);
	isnumber(ch)?(("printf alpha is an alphabet character")):(("printf number is not alphabet character"));
	return 0;
}
