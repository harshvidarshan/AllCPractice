//absolute value of number entered through keyboard//
#include<stdio.h>
int main()
{
		int a,b;
		printf("Enter number: ");
		scanf("%d",&a);
		if(a = -a && a<0 || a>0)
		printf("absolute value",a);
	    else
		printf("not absolute value");
		return 0;
}
